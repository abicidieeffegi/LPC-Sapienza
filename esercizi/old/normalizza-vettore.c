#include <stdio.h>
#include <math.h>
#include <stdlib.h>
void normalizza(double* vettore) {

}

int main() {
  double *numeri;
  int n;
  printf("Quanto ce l'hai lungo? ");
  scanf(" %d", &n);
  numeri = malloc(n * sizeof(*numeri));

  return 0;
}
